// --------------------------------------------------------------------------
// 실습: 반복문 (Loops & Iteration)
// --------------------------------------------------------------------------
// * while : 조건이 참인 동안 계속해서 방을 점검합니다.
// * for : 정해진 횟수만큼 손님에게 인사합니다.
// * for...of : 배열 장부의 손님 이름을 하나씩 꺼냅니다.
// * for...in : 객체 장부의 상세 정보(Key)를 확인합니다.
// --------------------------------------------------------------------------

const draw = (n) => '화면에 ' + n + '번째 그림을 그리다.'

// 화면에 [n]번 드로잉하고 싶다.
let count = 0
const REPEAT_COUNT = 5 // n번

while (count < REPEAT_COUNT) {
  count += 1 // [1], [2], 3, 4, [5]

  // continue : 건너띄워서 계속
  if (count >= 3 && count <= 4) {
    // continue // 현재 코드에서 아래 코드 건너뛰고 다시 while 조건 확인 이동
    break
  }
  draw(count)
  console.log(count)
}




/*
count = count + 1 // 값 변경
console.log(draw(count)) // 값 출력

count = count + 1 // 값 변경
console.log(draw(count)) // 값 출력

count = count + 1 // 값 변경
console.log(draw(count)) // 값 출력

count = count + 1 // 값 변경
console.log(draw(count)) // 값 출력
*/

// --------------------------------------------------------------------------
// while 문 - if문과 작성방법이 같음
// --------------------------------------------------------------------------

let ifCount = 0


// if...else 문 (조건 흐름 제어문)
// 조건: 반복 횟수가 5보다 작은 경우 > 참
// 조건이 참이면 코드 블록 실행 (1회)

if (ifCount < 5) {
  ifCount = ifCount + 1
  console.log(ifCount)
}


// while 문 (반복 흐름 제어문)
// 조건: 반복 횟수가 5보다 작을 경우 "참"
// 조건이 "참"이면 코드 블록 반복 실행 (조건이 참일 동안)
let loopCount = 0

/*
let dontOver = true
while (dontOver) {
  if (loopCount++ >= 5) {
    console.log(loopCount)
    dontOver = true
  }
  console.log(loopCount)
  loopCount +=2
}
  */


while (loopCount++ < 5) {
  //loopCount = loopCount + 1
  // loopCount++
  // LoopCount += 1
  // console.log(++loopCount)
  console.log(loopCount)
}

/*
while(조건){
     조건이 참인 동안 블록 내부의 코드 실행
}

- if 문과 작성 방법 같음

- 반복문이 멈추지 않으면?
- JavaScript는 한번에 하나의 일만 할 수 있어, 먼저 한 일이 멈추지 않으면 다음 일을 하지 못해요.
- "무한반복(infinite Loop)에 빠지지 않게"
- 반복문 사용 시 주의가 필요합니다.
*/

console.log('언제 멈춰??😡')

// 연명부
const people = ['한돌', '두돌', '세돌', '네돌']

// 연명부를 반복 순환
// 찾는 사람이 있다면 반복을 그만 멈춰라.
const who = '두돌'

// 반복문 
let index = 0, personName = ''
while (index < people.length /*0 < 4*/) {
  personName = people.at(index) // '한돌'

  // 검색한 who와 personName이 같다면?
  // 반복을 멈춰라.
  if (personName === who) break
  
  // 반복문 중단되면 아래 코드는 실행 안되요.
  index += 1
}

console.log(personName)

console.log(people.length)


// --------------------------------------------------------------------------
// do...while 문
// --------------------------------------------------------------------------

// while (조건) {
//   블록 안 실행할 코드
// }

// do it : ~를 한다 (조건과 상관없이 반드시 1회는 실행)
// while : ~동안 (반복: 조건이 참인 동인)


// do {
//   블록 안에 실행할 코드
// } while(조건)

let i = 0 // 반복횟수: iteractionCount
/*while (i < 10) {
  console.log(i)
  i += 1
}

console.log(i)
*/

do {
  console.log(i)
  i += 1
} while (i < 10)
console.log(i)
  

// --------------------------------------------------------------------------
// for 문
// --------------------------------------------------------------------------
 
// while 문 -> for 문


// while 문
const TARGET_COUNT = 20 // 목표 값

{
let whileCount = 0 // 반복 횟수

// 20번 반복 (0~19)
while (whileCount < TARGET_COUNT) {
  console.log(whileCount)
  whileCount += 1
  }

  console.log(whileCount)
}

// for 문
{

  // let forCount = 0

  for (let forCount = 0; forCount < TARGET_COUNT; forCount += 1) {
    console.log(forCount)
  }


  // 전형적인 for 문
  for (let i = 0; i <= 9; i++){
    console.log(i)
  }
}

// --------------------------------------------------------------------------
// 배열 반복 (역순/정순)
// --------------------------------------------------------------------------

const numbers = ['하나', '둘', '셋', '넷', '다섯', '여섯', '일곱']
//                 0      1    2     3      4     5       6

// 배열 순환
{
  // while
  let i = 0

  while (i < numbers.length) {
    const number = numbers[i]
    console.log(number)
    i++
  }
}

{
  // do...while

  let i = 0

  do {
    const number = numbers[i]
    console.log(number)
    i++

  }while(i < numbers.length)
}

{
  // for 문

  for (i = 0; i < numbers.length; i++){
    const number = numbers[i]
    console.log(number)
  }
}


// --------------------------------------------------------------------------
// for...of 문
// --------------------------------------------------------------------------


// --------------------------------------------------------------------------
// for...in 문 (객체 반복)
// --------------------------------------------------------------------------


// --------------------------------------------------------------------------
// 핵심 요약!
// --------------------------------------------------------------------------
// 1. while 문은 반복 횟수가 정해지지 않았을 때 유리합니다.
// 2. for 문은 인덱스(i)가 필요하거나 정해진 범위 반복에 최적입니다.
// 3. 배열 순회 시에는 가독성이 좋은 for...of를 적극 활용하세요.
// 4. 객체의 속성을 훑을 때는 for...in을 사용합니다.
// 5. break는 루프 탈출, continue는 이번 차례만 건너뛰기입니다.
// --------------------------------------------------------------------------